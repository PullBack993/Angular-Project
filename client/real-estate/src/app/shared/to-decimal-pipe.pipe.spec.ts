import { ToDecimalPipePipe } from './to-decimal-pipe.pipe';

describe('ToDecimalPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ToDecimalPipePipe();
    expect(pipe).toBeTruthy();
  });
});
